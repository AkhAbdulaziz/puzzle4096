<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uz.gita.puzzle4096">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher5"
        android:label="@string/app_name"
        android:name="uz.gita.puzzle4096.app.App"
        android:roundIcon="@mipmap/ic_launcher5_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson33">
        <activity
            android:screenOrientation="portrait"
            android:name="uz.gita.puzzle4096.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>